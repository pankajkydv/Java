
public class StringPoolExperiment {
  
    public static void main(String[] args) {
        String s1 = "Rachel";
        String s2 = "Rachel";
        String s3 = new String("Rachel");//creates two objects, one in string constant pool and other in non-pool(heap). and one reference variable.The variable s will refer to the object in a heap (non-pool).
        //if you are willing to add this string to the string literal pool, Java provides you with a method called, intern() method; you can call native intern() method like this:
        String s4 = new String("Rachel").intern();
  
        System.out.println(s1 == s2); // true
        System.out.println(s1 == s3); // false
        System.out.println(s1 == s4); // true
    }
}
